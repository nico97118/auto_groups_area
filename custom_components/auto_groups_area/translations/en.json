{
  "config": {
    "step": {
      "user": {
        "title": "Auto Groups by Area",
        "description": "Automatically create and keep updated one light group per area.",
        "data": {}
      }
    },
    "abort": {
      "already_configured": "This integration is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Options",
        "description": "Tune which groups are created and how sensor values are aggregated.",
        "data": {
          "actuator_domains": "Actuators to group (domains)",
          "enable_binary_sensors": "Create binary_sensor groups",
          "enable_sensors": "Create sensor groups",
          "group_prefix": "Entity id prefix (object id)",
          "create_when_empty": "Create groups even when empty",
          "include_device_area": "Include entities whose device is assigned to the area",
          "included_areas": "Only include these areas",
          "excluded_areas": "Exclude these areas"
        }
      },
      "binary_sensors": {
        "title": "Binary Sensors",
        "description": "Choose which binary_sensor groups to create (only applies if binary_sensor groups are enabled).",
        "data": {
          "enable_binary_sensor_motion": "Group: motion",
          "enable_binary_sensor_presence": "Group: presence",
          "enable_binary_sensor_opening": "Group: opening",
          "enable_binary_sensor_openclose": "Group: door/window"
        }
      },
      "sensors": {
        "title": "Sensors",
        "description": "Choose how values are aggregated (only applies if sensor groups are enabled).",
        "data": {
          "illuminance_aggregation": "Illuminance aggregation",
          "humidity_aggregation": "Humidity aggregation",
          "temperature_aggregation": "Temperature aggregation"
        }
      },
      "advanced": {
        "title": "Advanced",
        "description": "Advanced filters.",
        "data": {
          "excluded_entities": "Excluded entities"
        }
      }
    }
  }
}
