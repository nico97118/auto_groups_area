# Tests

This folder contains the integration test suite based on `pytest` +
`pytest-homeassistant-custom-component`.

See `TESTING.md` at the repository root for the full QA plan and commands (tests, lint, formatting, pre-commit).
